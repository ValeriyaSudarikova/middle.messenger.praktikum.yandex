<div class="settings__input-wrapper">
    <label class="settings__input-label fz-25">
        {{label}}
        <span class="settings__input-label subtitle"> {{subtitle}} </span>
    </label>
    <input type="{{type}}" name="{{name}}" placeholder="{{placeholder}}" required value="{{value}}">
</div>